export async function GET() {
  return Response.json({ message: 'Payments endpoint — coming soon' });
}

export async function POST() {
  return Response.json({ message: 'Payments endpoint — coming soon' });
}
